version: '3'
services:
  web:
    build: .
    command: npm start
    volumes:
      - .:/usr/app/
      - /usr/app/node_modules
    ports:
      - "3000:3000"
    stdin_open: true
    tty: true
    # depends_on:
    #   - postgres
    # environment:
    #   DATABASE_URL: postgres://todoapp@postgres/todos
  # postgres:
  #   image: postgres:11
  #   environment:
  #     POSTGRES_USER: todoapp
  #     POSTGRES_DB: todos
